def extract_names(string,septr):
    ''' extract_names function takes two arguments and
       the first argument is the string and the other argument 
       is the seperator'''
    return string.split(septr)